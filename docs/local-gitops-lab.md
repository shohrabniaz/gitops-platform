# Local GitOps lab

```powershell
git clone https://github.com/shohrabniaz/gitops-platform.git
# From your workspace that includes scripts/:
.\scripts\bootstrap-gitops-local.ps1 -UseKind
```

See the main portfolio workspace `docs/local-gitops-lab.md` for full steps.
